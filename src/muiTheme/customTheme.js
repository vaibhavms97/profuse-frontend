import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#4848e9',
    },
    secondary: {
      main: '#f50057',
    },
    text: {
      primary: '#000000',
    },
  },
});

export default theme